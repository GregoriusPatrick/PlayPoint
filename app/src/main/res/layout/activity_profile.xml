<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ProfileActivity">

    <!-- Kontainer utama untuk konten profil, ditempatkan di atas Bottom Navigation -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center_horizontal"
        android:padding="16dp"
        android:layout_above="@+id/bottom_navigation">

        <!-- Tempat Foto Profil -->
        <ImageView
            android:id="@+id/profile_image"
            android:layout_width="120dp"
            android:layout_height="120dp"
            android:layout_marginTop="32dp"
            android:src="@drawable/ic_profile"
            android:scaleType="centerCrop"
            android:contentDescription="Foto Profil" />

        <!-- Username -->
        <TextView
            android:id="@+id/username_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:text="Username"
            android:textSize="24sp"
            android:textStyle="bold"
            android:textColor="?android:attr/textColorPrimary" />

        <!-- Divider -->
        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:background="?android:attr/listDivider"
            android:layout_marginTop="24dp"
            android:layout_marginBottom="16dp"/>

        <!-- Opsi Menu (ID Game, Pengaturan, dll) -->
        <TextView
            android:id="@+id/voucher_code"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Redeem Voucher Code"
            android:textSize="18sp"
            android:padding="12dp"
            android:drawableStart="@drawable/ic_voucher"
            android:drawablePadding="16dp"
            android:gravity="center_vertical"/>

        <TextView
            android:id="@+id/settings_profile"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Settings"
            android:textSize="18sp"
            android:padding="12dp"
            android:layout_marginTop="8dp"
            android:drawableStart="@drawable/ic_settings"
            android:drawablePadding="16dp"
            android:gravity="center_vertical"/>

        <TextView
            android:id="@+id/customer_servicer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Customer Service"
            android:textSize="18sp"
            android:padding="12dp"
            android:layout_marginTop="8dp"
            android:drawableStart="@drawable/ic_cs"
            android:drawablePadding="16dp"
            android:gravity="center_vertical"/>

    </LinearLayout>

    <!-- Bottom Navigation Bar -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_navigation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        app:menu="@menu/bottom_menu" />

</RelativeLayout>
